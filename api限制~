    虽然LPC对字符串的处理较强（它本身的指令都是用的字符串），但却无法处理字符。显然这对遂个解析字符串很不友好，
因此我都是分解为字符串(虽然这个字符串只有一个字母)来处理。另外，不同的mudlib，可能efun也不一样，或许你所用的lib
在某些地方能有更好的函数来处理。下面列举我所用mudlib部分限制：
1.无法处理char型，也不提供efun
2.无进制转换，对于json中\u的转换只能自己实现。
3.对于数据类型的判断，isdigit之类的函数不可用。故只能用正则表达式。
